name: Multiplayer E2E Lab

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'package.json'
      - 'playwright.config.mjs'
      - 'realtime-hello-throttle-v404.js'
      - 'multiplayer-v435-loader.js'
      - 'multiplayer-test-hooks-v434.js'
      - 'scripts/run-e2e-route-gate.mjs'
      - 'tests/e2e/**'
      - '.github/workflows/multiplayer-e2e-lab.yml'
  pull_request:
    branches: [main]
    paths:
      - 'index.html'
      - 'package.json'
      - 'playwright.config.mjs'
      - 'realtime-hello-throttle-v404.js'
      - 'multiplayer-v435-loader.js'
      - 'multiplayer-test-hooks-v434.js'
      - 'scripts/run-e2e-route-gate.mjs'
      - 'tests/e2e/**'
      - '.github/workflows/multiplayer-e2e-lab.yml'
  workflow_dispatch:
    inputs:
      delay_ms:
        description: Artificial one-way gameplay delay in milliseconds
        required: true
        default: '60'
        type: string
      jitter_ms:
        description: Artificial gameplay jitter in milliseconds
        required: true
        default: '20'
        type: string
      loss_pct:
        description: Artificial gameplay packet loss percentage
        required: true
        default: '2'
        type: string
      sample_duration_ms:
        description: Measurement duration in milliseconds
        required: true
        default: '15000'
        type: string

permissions:
  contents: read

concurrency:
  group: multiplayer-e2e-lab-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  two-player-lab:
    name: two-player-lab (${{ matrix.route }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        route: [p2p, turn]
    env:
      NET_DELAY: ${{ inputs.delay_ms || '60' }}
      NET_JITTER: ${{ inputs.jitter_ms || '20' }}
      NET_LOSS: ${{ inputs.loss_pct || '2' }}
      FORCE_TURN: ${{ matrix.route == 'turn' }}
      EXPECT_ROUTE: ${{ matrix.route == 'turn' && 'METERED' || 'P2P' }}
      SAMPLE_DURATION_MS: ${{ inputs.sample_duration_ms || '15000' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          package-manager-cache: false

      - name: Install test dependencies
        run: npm install --no-audit --no-fund

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run deterministic synchronization matrix
        run: npm run test:sync

      - name: Run two-player browser laboratory and verify route
        id: browser_lab
        continue-on-error: true
        run: node scripts/run-e2e-route-gate.mjs

      - name: Upload reports, traces and videos
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: multiplayer-e2e-${{ matrix.route }}-report-${{ github.run_number }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: warn
          retention-days: 14

      - name: Write test summary
        if: always()
        run: |
          {
            echo '## 泡泡島雙玩家測試 v4.3.5'
            echo ''
            echo "- 驗證路徑：${EXPECT_ROUTE}"
            echo "- 延遲：${NET_DELAY}ms"
            echo "- 抖動：${NET_JITTER}ms"
            echo "- 丟包：${NET_LOSS}%"
            echo "- 強制 TURN：${FORCE_TURN}"
            echo "- 量測時間：${SAMPLE_DURATION_MS}ms"
            echo "- 瀏覽器與路由驗證：${{ steps.browser_lab.outcome }}"
            echo ''
            echo 'P2P 工作會要求兩端 route=P2P；TURN 工作會要求兩端 route=METERED、relayOnlyAttempt=true。'
            echo '報告會分開顯示 rawDrift 與扣除視覺延遲後的 pathDrift。'
            echo '失敗時請下載對應 route artifact，查看 failure-diagnostics.json、Trace、截圖與影片。'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Fail workflow when browser laboratory or route gate failed
        if: steps.browser_lab.outcome != 'success'
        run: |
          echo "${EXPECT_ROUTE} 雙玩家瀏覽器或路由驗證失敗；完整狀態已上傳到 artifact。"
          exit 1
